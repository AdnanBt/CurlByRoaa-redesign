<style>
  .hero {
    position: relative;
    height: 100vh;
    min-height: 800px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    background: #000;
    margin-top: 80px; /* For fixed header */
  }

  .hero-video {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.3; /* Reduced opacity for better text visibility */
  }

  .hero-video-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      135deg,
      rgba(45, 45, 45, 0.7) 0%,    /* Darker overlay for better contrast */
      rgba(232, 196, 184, 0.4) 100%
    );
    z-index: 1;
  }

  .hero-content {
    position: relative;
    z-index: 2; /* Higher z-index to stay above overlay */
    text-align: center;
    color: white;
    max-width: 800px;
    padding: 0 20px;
  }

  .hero-title {
    font-size: 4.5rem;
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 24px;
    letter-spacing: -0.02em;
    opacity: 0;
    transform: translateY(30px);
    animation: fadeInUp 1s ease 0.3s forwards;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5); /* Text shadow for better readability */
  }

  .hero-subtitle {
    font-size: 1.5rem;
    line-height: 1.6;
    margin-bottom: 48px;
    color: rgba(255, 255, 255, 0.95); /* Brighter text */
    opacity: 0;
    transform: translateY(30px);
    animation: fadeInUp 1s ease 0.6s forwards;
    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.4); /* Text shadow */
  }

  .hero-actions {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
    opacity: 0;
    transform: translateY(30px);
    animation: fadeInUp 1s ease 0.9s forwards;
    position: relative;
    z-index: 3;
  }

  .hero-btn {
    padding: 16px 32px;
    font-size: 1rem;
    font-weight: 600;
    text-decoration: none;
    border-radius: 50px;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    text-transform: uppercase;
    letter-spacing: 0.5px;
    position: relative;
    overflow: hidden;
    backdrop-filter: blur(10px); /* Glass effect */
    border: 1px solid rgba(255, 255, 255, 0.2); /* Subtle border */
  }

  .hero-btn-primary {
    background: rgba(232, 196, 184, 0.9); /* More opaque */
    color: #2D2D2D;
    border: 2px solid #E8C4B8;
  }

  .hero-btn-primary:hover {
    background: rgba(232, 196, 184, 1);
    color: #2D2D2D;
    transform: translateY(-2px);
    box-shadow: 0 10px 30px rgba(232, 196, 184, 0.4);
  }

  .hero-btn-secondary {
    background: rgba(255, 255, 255, 0.15); /* Semi-transparent */
    color: white;
    border: 2px solid rgba(255, 255, 255, 0.4);
    backdrop-filter: blur(10px);
  }

  .hero-btn-secondary:hover {
    background: rgba(255, 255, 255, 0.25);
    color: white;
    transform: translateY(-2px);
    border-color: rgba(255, 255, 255, 0.6);
    box-shadow: 0 10px 30px rgba(255, 255, 255, 0.2);
  }

  .hero-stats {
    display: flex;
    justify-content: center;
    gap: 60px;
    margin-top: 80px;
    padding-top: 40px;
    border-top: 1px solid rgba(255, 255, 255, 0.3);
    opacity: 0;
    animation: fadeIn 1s ease 1.2s forwards;
    position: relative;
    z-index: 2;
  }

  .stat-item {
    text-align: center;
  }

  .stat-number {
    font-size: 2.5rem;
    font-weight: 700;
    display: block;
    color: #E8C4B8;
    margin-bottom: 8px;
    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
  }

  .stat-label {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.9);
    text-transform: uppercase;
    letter-spacing: 1px;
    text-shadow: 0 1px 4px rgba(0, 0, 0, 0.3);
  }

  .hero-scroll-indicator {
    position: absolute;
    bottom: 40px;
    left: 50%;
    transform: translateX(-50%);
    color: rgba(255, 255, 255, 0.8);
    animation: bounce 2s infinite;
    z-index: 2;
    background: rgba(0, 0, 0, 0.3);
    border-radius: 50%;
    padding: 8px;
    backdrop-filter: blur(5px);
  }

  .hero-fallback-image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: none;
    opacity: 0.4; /* Same opacity as video for consistency */
  }

  /* Content background for extra readability (optional) */
  .hero-content-backdrop {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 90%;
    max-width: 900px;
    height: 70%;
    background: rgba(45, 45, 45, 0.3);
    border-radius: 20px;
    backdrop-filter: blur(20px);
    z-index: 1;
    border: 1px solid rgba(255, 255, 255, 0.1);
    display: none; /* Hidden by default, can enable if needed */
  }

  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeIn {
    to {
      opacity: 1;
    }
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateX(-50%) translateY(0);
    }
    40% {
      transform: translateX(-50%) translateY(-10px);
    }
    60% {
      transform: translateX(-50%) translateY(-5px);
    }
  }

  /* Mobile Styles */
  @media (max-width: 768px) {
    .hero {
      min-height: 700px;
      height: 100vh;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .hero-subtitle {
      font-size: 1.25rem;
    }

    .hero-actions {
      flex-direction: column;
      align-items: center;
    }

    .hero-btn {
      width: 200px;
    }

    .hero-stats {
      flex-direction: column;
      gap: 30px;
      margin-top: 60px;
    }

    .stat-number {
      font-size: 2rem;
    }

    /* Stronger overlay on mobile for better readability */
    .hero-video-overlay {
      background: linear-gradient(
        135deg,
        rgba(45, 45, 45, 0.8) 0%,
        rgba(232, 196, 184, 0.3) 100%
      );
    }
  }

  @media (max-width: 480px) {
    .hero-title {
      font-size: 2rem;
    }

    .hero-subtitle {
      font-size: 1.125rem;
    }
  }
</style>

<section class="hero">
  <!-- Optional content backdrop (uncomment if needed) -->
  <!-- <div class="hero-content-backdrop"></div> -->
  
  <!-- Video Background -->
  <video class="hero-video" autoplay muted loop playsinline 
         onerror="document.getElementById('hero-fallback').style.display='block'; this.style.display='none';">
    <source src="https://cdn.shopify.com/videos/c/o/v/1f8218940a0d4c5bbd92cb24c024b184.mp4" type="video/mp4">
    Your browser does not support the video tag.
  </video>
  
  <!-- Fallback Image -->
  <img 
    id="hero-fallback"
    class="hero-fallback-image"
    src="https://cdn.shopify.com/s/files/1/0567/4172/4316/files/2211B086-44F7-40E0-BE64-96D504EAE7F9.webp?v=1763925101" 
    alt="Hero background - CurlsByRoaa hair care products"
    width="1920"
    height="1080"
    loading="eager"
  >
  
  <div class="hero-video-overlay"></div>

  <div class="hero-content">
    <h1 class="hero-title">
      {{ section.settings.heading | default: "Discover Your Perfect Curl Journey" }}
    </h1>
    
    <p class="hero-subtitle">
      {{ section.settings.subheading | default: "Professional-grade hair care solutions tailored to your unique curl pattern. Transform your routine with scientifically formulated products." }}
    </p>
    
    <div class="hero-actions">
      <a href="{{ section.settings.cta_url | default: '/collections/all' }}" class="hero-btn hero-btn-primary">
        {{ section.settings.cta_text | default: 'Shop The Collection' }}
      </a>
      <a href="{{ section.settings.secondary_cta_url | default: '/pages/quiz' }}" class="hero-btn hero-btn-secondary">
        {{ section.settings.secondary_cta_text | default: 'Find Your Routine' }}
      </a>
    </div>

    <div class="hero-stats">
      <div class="stat-item">
        <span class="stat-number">10K+</span>
        <span class="stat-label">Happy Customers</span>
      </div>
      <div class="stat-item">
        <span class="stat-number">4.9★</span>
        <span class="stat-label">Average Rating</span>
      </div>
      <div class="stat-item">
        <span class="stat-number">100%</span>
        <span class="stat-label">Natural Ingredients</span>
      </div>
    </div>
  </div>

  <div class="hero-scroll-indicator">
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M12 5v14M5 12l7 7 7-7"/>
    </svg>
  </div>
</section>

<script>
document.addEventListener('DOMContentLoaded', function() {
  // Scroll functionality
  const scrollIndicator = document.querySelector('.hero-scroll-indicator');
  if (scrollIndicator) {
    scrollIndicator.addEventListener('click', function() {
      window.scrollTo({
        top: window.innerHeight,
        behavior: 'smooth'
      });
    });
  }

  // Video error handling
  const heroVideo = document.querySelector('.hero-video');
  const fallbackImage = document.getElementById('hero-fallback');
  
  if (heroVideo) {
    heroVideo.addEventListener('error', function() {
      console.log('Video failed to load, showing fallback image');
      if (fallbackImage) {
        fallbackImage.style.display = 'block';
        this.style.display = 'none';
      }
    });

    heroVideo.addEventListener('loadeddata', function() {
      console.log('Video loaded successfully');
      if (fallbackImage) {
        fallbackImage.style.display = 'none';
      }
    });
  }

  // Fallback timeout
  setTimeout(function() {
    if (heroVideo && heroVideo.readyState < 3) {
      console.log('Video taking too long to load, showing fallback');
      if (fallbackImage) {
        fallbackImage.style.display = 'block';
        heroVideo.style.display = 'none';
      }
    }
  }, 3000);
});
</script>

{% schema %}
{
  "name": "Hero Section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Discover Your Perfect Curl Journey"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Professional-grade hair care solutions tailored to your unique curl pattern"
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "Primary Button Text",
      "default": "Shop The Collection"
    },
    {
      "type": "url",
      "id": "cta_url",
      "label": "Primary Button Link"
    },
    {
      "type": "text",
      "id": "secondary_cta_text",
      "label": "Secondary Button Text",
      "default": "Find Your Routine"
    },
    {
      "type": "url",
      "id": "secondary_cta_url",
      "label": "Secondary Button Link"
    }
  ],
  "presets": [
    {
      "name": "Numberc Style Hero",
      "category": "Homepage"
    }
  ]
}
{% endschema %}