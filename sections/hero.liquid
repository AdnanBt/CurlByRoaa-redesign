<style>
  .hero {
    position: relative;
    height: 80vh;
    min-height: 600px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    background: #0a0a0a;
    margin-top: 80px;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 50%, #2a1e1c 100%);
  }

  /* Single Card Focus Design */
  .hero-carousel {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .carousel-track {
    display: flex;
    gap: 40px;
    animation: carouselScroll 40s linear infinite;
    padding: 0 50px;
  }

  .carousel-track:hover {
    animation-play-state: paused;
  }

  .product-card-hero {
    flex: 0 0 420px;
    height: 520px;
    border-radius: 28px;
    padding: 50px 35px;
    text-align: center;
    backdrop-filter: blur(25px);
    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }

  /* Main Content Card Styles */
  .hero-content-card {
    background: linear-gradient(145deg, 
      rgba(0, 0, 0, 0.8) 0%, 
      rgba(0, 0, 0, 0.6) 100%);
    border: 1px solid rgba(255, 255, 255, 0.2);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }

  /* Different color themes for product cards */
  .product-card-hero:nth-child(8n+2),
  .product-card-hero:nth-child(8n+6) {
    background: linear-gradient(145deg, 
      rgba(232, 196, 184, 0.15) 0%, 
      rgba(232, 196, 184, 0.05) 100%);
    border: 1px solid rgba(232, 196, 184, 0.3);
  }

  .product-card-hero:nth-child(8n+3),
  .product-card-hero:nth-child(8n+7) {
    background: linear-gradient(145deg, 
      rgba(168, 216, 234, 0.15) 0%, 
      rgba(168, 216, 234, 0.05) 100%);
    border: 1px solid rgba(168, 216, 234, 0.3);
  }

  .product-card-hero:nth-child(8n+4),
  .product-card-hero:nth-child(8n+8) {
    background: linear-gradient(145deg, 
      rgba(149, 225, 211, 0.15) 0%, 
      rgba(149, 225, 211, 0.05) 100%);
    border: 1px solid rgba(149, 225, 211, 0.3);
  }

  .product-card-hero:nth-child(8n+5),
  .product-card-hero:nth-child(8n+9) {
    background: linear-gradient(145deg, 
      rgba(255, 154, 158, 0.15) 0%, 
      rgba(255, 154, 158, 0.05) 100%);
    border: 1px solid rgba(255, 154, 158, 0.3);
  }

  .product-card-hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
    transition: left 0.8s ease;
  }

  .product-card-hero:hover::before {
    left: 100%;
  }

  .product-card-hero:hover {
    transform: translateY(-20px) scale(1.08);
    background: rgba(255, 255, 255, 0.12);
    box-shadow: 
      0 30px 60px rgba(0, 0, 0, 0.5),
      0 0 0 1px rgba(255, 255, 255, 0.2);
  }

  .product-image-hero {
    width: 220px;
    height: 220px;
    border-radius: 50%;
    object-fit: cover;
    margin: 0 auto 35px;
    border: 3px solid rgba(255, 255, 255, 0.2);
    transition: all 0.5s ease;
    background: linear-gradient(135deg, #2a1e1c, #3a2e2c);
  }

  .product-card-hero:hover .product-image-hero {
    transform: scale(1.2);
    border-color: rgba(255, 255, 255, 0.4);
  }

  .product-title-hero {
    font-size: 1.5rem;
    font-weight: 400;
    color: white;
    margin-bottom: 20px;
    line-height: 1.4;
    letter-spacing: -0.5px;
  }

  .product-price-hero {
    font-size: 1.8rem;
    font-weight: 300;
    color: #E8C4B8;
    margin-bottom: 30px;
    font-family: 'Inter', sans-serif;
  }

  .product-badge-hero {
    position: absolute;
    top: 25px;
    right: 25px;
    background: rgba(255, 255, 255, 0.9);
    color: #000;
    padding: 8px 16px;
    border-radius: 20px;
    font-size: 0.85rem;
    font-weight: 600;
    backdrop-filter: blur(10px);
  }

  /* Hero Content Styles */
  .hero-title {
    font-size: 3rem;
    font-weight: 350;
    line-height: 1.1;
    margin-bottom: 20px;
    letter-spacing: -0.02em;
    background: linear-gradient(135deg, #ffffff 0%, #E8C4B8 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-align: center;
  }

  .hero-subtitle {
    font-size: 1.2rem;
    line-height: 1.6;
    margin-bottom: 40px;
    color: rgba(255, 255, 255, 0.8);
    font-weight: 350;
    max-width: 500px;
    text-align: center;
  }

  .hero-actions {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .hero-btn {
    padding: 16px 36px;
    font-size: 0.9rem;
    font-weight: 400;
    text-decoration: none;
    border-radius: 12px;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    text-transform: uppercase;
    letter-spacing: 1.2px;
    border: 1px solid rgba(255, 255, 255, 0.3);
    background: transparent;
    color: white;
  }

  .hero-btn-primary {
    background: white;
    color: #000;
    border: 1px solid white;
  }

  .hero-btn-primary:hover {
    background: transparent;
    color: white;
    transform: translateY(-2px);
    box-shadow: 0 15px 35px rgba(255, 255, 255, 0.15);
  }

  .hero-btn-secondary {
    background: transparent;
    color: white;
    border: 1px solid rgba(255, 255, 255, 0.3);
  }

  .hero-btn-secondary:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.5);
    transform: translateY(-2px);
  }

  .hero-scroll-indicator {
    position: absolute;
    bottom: 30px;
    left: 50%;
    transform: translateX(-50%);
    color: rgba(255, 255, 255, 0.6);
    animation: float 3s ease-in-out infinite;
    z-index: 20;
    cursor: pointer;
    background: rgba(0, 0, 0, 0.6);
    border-radius: 50%;
    padding: 12px;
    backdrop-filter: blur(15px);
    border: 1px solid rgba(255, 255, 255, 0.15);
  }

  /* Animations */
  @keyframes carouselScroll {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(calc(-420px * 5 - 40px * 5)); /* Adjusted for 5 cards */
    }
  }

  @keyframes float {
    0%, 100% {
      transform: translateX(-50%) translateY(0);
    }
    50% {
      transform: translateX(-50%) translateY(-8px);
    }
  }

  /* Mobile Styles */
  @media (max-width: 768px) {
    .hero {
      height: 70vh;
      min-height: 500px;
      margin-top: 60px;
    }

    .carousel-track {
      gap: 25px;
      padding: 0 30px;
      animation-duration: 30s;
    }

    .product-card-hero {
      flex: 0 0 320px;
      height: 420px;
      padding: 35px 25px;
    }

    .product-image-hero {
      width: 160px;
      height: 160px;
    }

    .hero-title {
      font-size: 2.2rem;
    }

    .hero-subtitle {
      font-size: 1rem;
      margin-bottom: 30px;
    }

    .hero-actions {
      flex-direction: column;
      align-items: center;
      gap: 12px;
    }

    .hero-btn {
      width: 200px;
      padding: 14px 28px;
    }

    @keyframes carouselScroll {
      0% {
        transform: translateX(0);
      }
      100% {
        transform: translateX(calc(-320px * 5 - 25px * 5));
      }
    }
  }
</style>

<section class="hero">
  <!-- Background -->
  <div class="hero-background"></div>

  <!-- Product Cards Carousel -->
  <div class="hero-carousel">
    <div class="carousel-track">
      <!-- Main Content Card - First in carousel -->
      <div class="product-card-hero hero-content-card">
        <h1 class="hero-title">
          {{ section.settings.heading | default: "Transform Your Curl Journey" }}
        </h1>
        
        <p class="hero-subtitle">
          {{ section.settings.subheading | default: "Experience the perfect harmony of science and nature with our meticulously crafted hair care solutions" }}
        </p>
        
        <div class="hero-actions">
          <a href="{{ section.settings.cta_url }}" class="hero-btn hero-btn-primary">
            {{ section.settings.cta_text | default: "Shop Collection" }}
          </a>
          <a href="{{ section.settings.secondary_cta_url }}" class="hero-btn hero-btn-secondary">
            {{ section.settings.secondary_cta_text | default: "Learn More" }}
          </a>
        </div>
      </div>

      <!-- Product Card 1 -->
      <div class="product-card-hero">
        <div class="product-badge-hero">BESTSELLER</div>
        <img 
          src="https://cdn.shopify.com/s/files/1/0567/4172/4316/files/product1.jpg" 
          alt="Curl Defining Cream"
          class="product-image-hero"
          loading="lazy"
          width="200"
          height="200"
        >
        <h3 class="product-title-hero">Curl Defining Cream</h3>
        <div class="product-price-hero">{{ 29.99 | money }}</div>
      </div>

      <!-- Product Card 2 -->
      <div class="product-card-hero">
        <div class="product-badge-hero">NEW</div>
        <img 
          src="https://cdn.shopify.com/s/files/1/0567/4172/4316/files/product2.jpg" 
          alt="Hydrating Shampoo"
          class="product-image-hero"
          loading="lazy"
          width="200"
          height="200"
        >
        <h3 class="product-title-hero">Hydrating Shampoo</h3>
        <div class="product-price-hero">{{ 24.99 | money }}</div>
      </div>

      <!-- Product Card 3 -->
      <div class="product-card-hero">
        <img 
          src="https://cdn.shopify.com/s/files/1/0567/4172/4316/files/product3.jpg" 
          alt="Repairing Conditioner"
          class="product-image-hero"
          loading="lazy"
          width="200"
          height="200"
        >
        <h3 class="product-title-hero">Repairing Conditioner</h3>
        <div class="product-price-hero">{{ 26.99 | money }}</div>
      </div>

      <!-- Product Card 4 -->
      <div class="product-card-hero">
        <div class="product-badge-hero">BESTSELLER</div>
        <img 
          src="https://cdn.shopify.com/s/files/1/0567/4172/4316/files/product4.jpg" 
          alt="Leave-In Treatment"
          class="product-image-hero"
          loading="lazy"
          width="200"
          height="200"
        >
        <h3 class="product-title-hero">Leave-In Treatment</h3>
        <div class="product-price-hero">{{ 32.99 | money }}</div>
      </div>

      <!-- Duplicate cards for seamless loop -->
      <!-- Main Content Card Duplicate -->
      <div class="product-card-hero hero-content-card">
        <h1 class="hero-title">
          {{ section.settings.heading | default: "Transform Your Curl Journey" }}
        </h1>
        
        <p class="hero-subtitle">
          {{ section.settings.subheading | default: "Experience the perfect harmony of science and nature with our meticulously crafted hair care solutions" }}
        </p>
        
        <div class="hero-actions">
          <a href="{{ section.settings.cta_url }}" class="hero-btn hero-btn-primary">
            {{ section.settings.cta_text | default: "Shop Collection" }}
          </a>
          <a href="{{ section.settings.secondary_cta_url }}" class="hero-btn hero-btn-secondary">
            {{ section.settings.secondary_cta_text | default: "Learn More" }}
          </a>
        </div>
      </div>

      <!-- Product Card 1 Duplicate -->
      <div class="product-card-hero">
        <div class="product-badge-hero">BESTSELLER</div>
        <img 
          src="https://cdn.shopify.com/s/files/1/0567/4172/4316/files/product1.jpg" 
          alt="Curl Defining Cream"
          class="product-image-hero"
          loading="lazy"
          width="200"
          height="200"
        >
        <h3 class="product-title-hero">Curl Defining Cream</h3>
        <div class="product-price-hero">{{ 29.99 | money }}</div>
      </div>

      <!-- Product Card 2 Duplicate -->
      <div class="product-card-hero">
        <div class="product-badge-hero">NEW</div>
        <img 
          src="https://cdn.shopify.com/s/files/1/0567/4172/4316/files/product2.jpg" 
          alt="Hydrating Shampoo"
          class="product-image-hero"
          loading="lazy"
          width="200"
          height="200"
        >
        <h3 class="product-title-hero">Hydrating Shampoo</h3>
        <div class="product-price-hero">{{ 24.99 | money }}</div>
      </div>

      <!-- Product Card 3 Duplicate -->
      <div class="product-card-hero">
        <img 
          src="https://cdn.shopify.com/s/files/1/0567/4172/4316/files/product3.jpg" 
          alt="Repairing Conditioner"
          class="product-image-hero"
          loading="lazy"
          width="200"
          height="200"
        >
        <h3 class="product-title-hero">Repairing Conditioner</h3>
        <div class="product-price-hero">{{ 26.99 | money }}</div>
      </div>

      <!-- Product Card 4 Duplicate -->
      <div class="product-card-hero">
        <div class="product-badge-hero">BESTSELLER</div>
        <img 
          src="https://cdn.shopify.com/s/files/1/0567/4172/4316/files/product4.jpg" 
          alt="Leave-In Treatment"
          class="product-image-hero"
          loading="lazy"
          width="200"
          height="200"
        >
        <h3 class="product-title-hero">Leave-In Treatment</h3>
        <div class="product-price-hero">{{ 32.99 | money }}</div>
      </div>
    </div>
  </div>

  <div class="hero-scroll-indicator" onclick="scrollToNextSection()">
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
      <path d="M12 5v14M5 12l7 7 7-7"/>
    </svg>
  </div>
</section>

<script>
function scrollToNextSection() {
  window.scrollTo({
    top: window.innerHeight,
    behavior: 'smooth'
  });
}
</script>

{% schema %}
{
  "name": "Hero Carousel",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Transform Your Curl Journey"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Experience the perfect harmony of science and nature with our meticulously crafted hair care solutions"
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "Primary Button Text",
      "default": "Shop Collection"
    },
    {
      "type": "url",
      "id": "cta_url",
      "label": "Primary Button Link"
    },
    {
      "type": "text",
      "id": "secondary_cta_text",
      "label": "Secondary Button Text",
      "default": "Learn More"
    },
    {
      "type": "url",
      "id": "secondary_cta_url",
      "label": "Secondary Button Link"
    }
  ],
  "presets": [
    {
      "name": "Hero Carousel",
      "category": "Homepage"
    }
  ]
}
{% endschema %}